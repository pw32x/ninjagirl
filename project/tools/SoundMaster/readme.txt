SoundMaster is used to take vgm sound assets and generate code for the project.

SoundMaster uses config files. 

The project's SoundMaster config file is located in the project's assets\vgm folder.

The config file has an .sm extension.

run buildsounds.bat to reexport all the vgms into code.