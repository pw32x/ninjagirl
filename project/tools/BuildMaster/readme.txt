BuildMaster is used to build assets and code for the project.

BuildMaster uses config files. 

The project's BuildMaster config file is located in the project's build folder.

The config file has a .bm extension.