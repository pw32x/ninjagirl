<UserControl x:Class="SceneMaster.SceneMasterDocumentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SceneMaster" 
             xmlns:viewmodels="clr-namespace:SceneMaster.ViewModels" xmlns:documents="clr-namespace:SceneMaster.Documents"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance viewmodels:SceneViewModel, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!--<Button Command="{Binding ModifyDocumentCommand}" Width="60" Height="20"/>-->
        <local:SceneView DataContext="{Binding SceneViewModel}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" VerticalContentAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
    </Grid>
</UserControl>
