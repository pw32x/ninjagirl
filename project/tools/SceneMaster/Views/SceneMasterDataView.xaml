<UserControl x:Class="SceneMaster.SceneMasterDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SceneMaster"
             mc:Ignorable="d" 
             d:DataContext ="{d:DesignInstance local:SceneMasterData, IsDesignTimeCreatable=True}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Background="Blue">
        <TextBlock Text="{Binding GalFilename}"/>
        <TextBlock Text="{Binding FrameInfos.Count}"/>
    </StackPanel>
</UserControl>
